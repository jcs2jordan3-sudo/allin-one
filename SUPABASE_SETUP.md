# Supabase 연동 설정 (약 10분)

키를 설정하기 전까지 앱은 **로컬 모드**(이 브라우저의 localStorage)로 동작합니다.
설정을 마치면 **클라우드 모드**가 켜지며 다음이 활성화됩니다.

- 직원 계정 로그인(대표/매니저/딜러 역할), 여러 기기 실시간 동기화
- 매장 QR → 손님이 휴대폰에서 **회원가입** (`/join`)
- 전광판 QR → 회원이 **포인트로 셀프 바인** (`/g/코드`) — 서버가 잔액 확인·차감·좌석 배정을 한 번에 처리
- 현금·카드 결제는 직원이 콘솔에서 포인트를 넣어주는 방식(포인트 내역 → 전송하기 → 사유 "현금 결제"/"카드 결제")

## 1. 프로젝트 만들기

1. https://supabase.com 접속 → 가입/로그인 (무료 플랜이면 충분)
2. **New Project** → 이름 아무거나(예: `allinone`), Region은 **Northeast Asia (Seoul)** 권장 → 생성 대기 (~2분)

## 2. 스키마 적용

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. 이 폴더의 `supabase/schema.sql` 파일 내용 전체를 붙여넣고 **Run**
   - 테이블·RLS 정책·원장 RPC·가입 트리거·Realtime 설정이 한 번에 만들어집니다
   - 여러 번 실행해도 안전합니다

## 3. 인증 설정 (중요)

왼쪽 메뉴 **Authentication → Providers → Email**:

- **Confirm email** 을 **끕니다** (OFF).
  켜 두면 손님이 가입 후 메일 인증 링크를 눌러야 로그인되므로 매장 현장에서는 불편합니다.
  (켜 두어도 동작은 하지만 가입 직후 바로 바인할 수 없습니다.)

**Authentication → URL Configuration** 에 배포 주소(예: `https://jcs2jordan3-sudo.github.io/allin-one/`)를 Site URL / Redirect URLs에 넣어 두면 인증 메일 링크가 올바른 곳으로 돌아옵니다.

## 4. 키 연결

1. 왼쪽 메뉴 **Project Settings → API**
2. `Project URL`과 `anon public` 키를 복사
3. 이 폴더(`app/`)에서 `.env.local.example`을 복사해 `.env.local` 파일을 만들고 값 입력:

```
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
```

4. 개발 서버 재시작: 터미널에서 Ctrl+C 후 `npm run dev` (또는 `시작하기.bat` 다시 실행)

## 5. 매장 개설

1. 앱을 열면 **"매장 개설"** 화면이 나옵니다. 매장 이름·대표 이름·이메일·비밀번호를 입력 → 이 계정이 대표(owner)가 됩니다.
2. 콘솔이 열리면 헤더에 초록 점 **"클라우드 동기화"** 가 표시됩니다.
3. **관리 탭 → 데이터 관리 → "데모 데이터로 초기화"** 를 누르면 시연용 회원·게임이 생성됩니다. 실영업은 "빈 상태로 시작".

## 6. 직원 추가

**관리 탭 → 직원 계정 → 직원 초대**: 이메일·이름·역할을 등록하면 "초대 대기" 상태가 됩니다.
그 직원이 콘솔 로그인 화면의 **"초대받은 직원이에요 → 가입"** 에서 같은 이메일로 가입하면 자동으로 연결됩니다.

| 역할 | 할 수 있는 것 |
|---|---|
| 대표(owner) | 전체. 직원 관리, 데이터 초기화 |
| 매니저(manager) | 게임 운영 + 재화 전송·환수(현금/카드 결제 후 포인트 지급), 회원 관리 |
| 딜러(dealer) | 게임 운영(참가 등록·탈락·종료)만. 재화 이동 불가 |

## 7. 손님 흐름 확인

1. **회원가입 QR**: 포인트 탭 또는 관리 탭의 "가입 QR" → 휴대폰으로 스캔 → 닉네임·전화번호·이메일·비밀번호로 가입
   - 직원이 미리 "회원 등록"으로 만들어 둔 회원은 **같은 전화번호**로 가입하면 자동 연결됩니다
2. **포인트 충전**: 카운터에서 현금/카드 결제 → 콘솔 "포인트 내역 → 전송하기" → 회원 검색 → 수량 → 사유 "현금 결제" 선택
3. **셀프 바인**: 타이머 전광판(또는 게임 카드의 "바인 QR")을 휴대폰으로 스캔 → 로그인 → "포인트 1P로 바인하기"
   - 좌석이 자동 배정되고 콘솔·전광판에 즉시 반영됩니다
   - 레지 마감 후, 잔액 부족, 규칙 한도 초과는 서버가 거부합니다
4. **내 정보**(`/me`): 회원번호·잔액·진행 중 게임·참가 기록·거래 내역

## 배포 (GitHub Pages)

anon 키는 공개용 키이므로 빌드에 포함해도 됩니다(RLS가 접근을 제한). `.env.local`이 있는 상태에서:

```
npm run build:pages
```

`dist/`를 gh-pages 브랜치에 올리면 됩니다. (base 경로 `/allin-one/`가 자동으로 붙습니다)

## 스키마 검증

Supabase 없이도 PGlite(WASM Postgres)로 `schema.sql` 전체 흐름을 검증할 수 있습니다:

```
npm run db:test
```

## 주의사항

- `.env.local`은 git에 올리지 마세요
- `service_role` 키는 절대 프론트에 넣지 마세요 (RLS를 우회합니다)
- 원장(ledger)은 수정·삭제가 차단되어 있습니다. 정정은 환수/전송 역거래로 처리하세요
- 전화번호 인증(SMS)으로 바꾸려면 Supabase Phone Provider(Twilio 등)를 연결하고 `auth.ts`의 `signUp/signIn`을 `signInWithOtp`로 바꾸면 됩니다. 스키마는 그대로 사용 가능합니다
